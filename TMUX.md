# TMUX Terminal Commands

* Open Tmux session :
 ```
tmux
```

* Split terminal :
```
shift + ctl +b  + % (vertical)
shift +ctl +b + " (horizontal)
  
```

* Navigate in pans
   ```
  ctl +b + o
  ctl + b + l
  ctl + b + ->
  ctl +b + <-

    ```
